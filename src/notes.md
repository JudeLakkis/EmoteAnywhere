Obtain Body Text
```javascript
var text = document.body.innerText
```

Text Splitting
```javascript
text.split(' ')
```

Stripping Characters in regex
```javascript
text.replace(/(\r\n|\n|\r)/gm, "")
```
